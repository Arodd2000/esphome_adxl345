wifi:
  id: wifi_component
  networks: !secret known_networks

  ap:
    ssid: "${name_verbose} Fallback"
    password: !secret fallback_password

captive_portal:

api:
  id: native_api

ota:

web_server:
  port: 80

